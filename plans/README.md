# Implementation Plans

Generated by the improve skill on 2026-06-10 (quick audit, commit `c234ef6`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

Repo facts every executor needs: macOS SwiftUI app, Swift 5.10, XcodeGen project.
Regenerate the project with `xcodegen generate`; build/test with
`xcodebuild -project SynapseMeetings.xcodeproj -scheme SynapseMeetings -destination 'platform=macOS' test`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Preserve summary on failed re-summarize | P1 | S | — | DONE |
| 002 | Autosave manual summary edits | P1 | S | — (001 recommended first) | DONE |
| 003 | Pipeline test seam + CI | P1 | M | 001 | DONE |
| 004 | Audio tap thread isolation (CaptureContext) | P2 | M | 003 (recommended) | DONE |
| 005 | Bounded live transcription (drain + append) | P2 | M | 004 (recommended) | TODO |
| 006 | Pipeline stale-snapshot merge | P2 | M | 003 (required) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **003 after 001**: 003 writes the regression test for 001's behavior
  (`testForcedResummarizeFailure_preservesOldSummary`). If 001 hasn't landed,
  003's executor skips that one test and marks it blocked here.
- **006 requires 003**: its lost-update test needs the `Summarizing` seam and the
  `PipelineExecutionTests` harness 003 creates. Do not run 006 early.
- **004 and 005 both restructure `AudioRecorder`'s PCM buffer.** Preferred order is
  004 → 005 (005 then implements its drain as `CaptureContext.drainSamples()`).
  If executed in reverse, 004's executor must port the drain into the context. Do
  not run them concurrently in parallel worktrees.
- 001 and 002 are independent of everything and safe to execute immediately.

## Findings considered and rejected / deferred

- **Cooperative cancellation inside FluidAudio transcription**: rejected — plan
  005's incremental design makes chunk work short instead; plumbing cancellation
  through a third-party inference loop is high effort, low payoff.
- **Removing `.id(recording.id)` in `ContentView`** as the fix for lost edits:
  rejected — changes view-identity semantics broadly; autosave (plan 002) is
  strictly safer.
- **Transcription/diarization injection seam**: deferred — no current test needs
  the transcription branch; add it when a plan actually exercises it (noted in 003).
- **Chunk overlap-and-merge for live-transcript boundary artifacts**: deferred —
  the authoritative final transcript is unaffected; revisit only on user
  complaints (noted in 005).
- **`RecordingStore.persist` failures only `print`** (`RecordingStore.swift:98`)
  and **`try!` in `baseDirectoryURL`** (`RecordingStore.swift:11`): noted, not
  planned — low likelihood, and surfacing store errors deserves a UX decision,
  not a drive-by.
- **Security category**: audited, no findings — keys live in the Keychain
  (`KeychainService`), all endpoints HTTPS, Sparkle feed EdDSA-signed, no secrets
  in the repo.
- **Not audited in this quick pass** (re-audit candidates for a `standard`/`deep`
  run): performance beyond the live-transcription path, dependency currency, DX,
  docs, direction/roadmap; views `SearchableTextEditor`, `SettingsView`,
  `CalendarSidebarView`, `AttendeesSidebarView` internals; `DiarizationService`,
  `GlobalHotkeyService`, `AudioDeviceService`, `AppMigration`.
